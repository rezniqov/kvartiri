services:
  app:
    image: backend-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: backend-prod
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
    restart: unless-stopped
