{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "migrate-dev": "bunx --bun prisma migrate dev --name init",
    "generate-dev": "bunx --bun prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "^7.7.0",
    "elysia": "latest",
    "pg": "^8.20.0",
    "prismabox": "^1.1.26"
  },
  "devDependencies": {
    "bun-types": "^1.3.12",
    "prisma": "^7.7.0"
  },
  "module": "src/index.js"
}
